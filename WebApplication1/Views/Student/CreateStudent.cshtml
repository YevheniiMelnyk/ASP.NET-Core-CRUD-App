@model Student

@{
    ViewData["Title"] = "Create student";
}

<div class="container">
    <h2>Group Creating</h2>
    <form asp-action="CreateStudent" asp-route-groupId="@Model.GROUP_ID" method="post">
       @* <div class="form-group">
            <label asp-for="GROUP_ID"></label>
            <input type="hidden" class="form-control" asp-for="GROUP_ID" />
            <span asp-validation-for="GROUP_ID" class="text-danger"></span>
        </div>*@

        <div class="form-group">
            <label asp-for="FIRST_NAME"></label>
            <input class="form-control" asp-for="FIRST_NAME" />
            <span asp-validation-for="FIRST_NAME" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="LAST_NAME"></label>
            <input class="form-control" asp-for="LAST_NAME" />
            <span asp-validation-for="LAST_NAME" class="text-danger"></span>
        </div>

        <input type="hidden" name="groupId" value="@Model.GROUP_ID" />

        <div class="form-group text-center">
            <button type="submit" class="btn btn-primary">Create</button>
            <a class="btn btn-secondary" asp-action="Students" asp-route-groupId="@Model.GROUP_ID">Cancel</a>
        </div>
    </form>
</div>